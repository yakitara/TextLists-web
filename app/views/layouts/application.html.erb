<!DOCTYPE html>
<html>
  <head>
    <%= hoptoad_javascript_notifier %>
    <!-- <meta http-equiv="content-type" content="text/html; charset=UTF-8"> -->
    <meta charset="UTF-8" />
    <!-- <meta name="viewport" content="width=device-width" /> -->
    <meta name="viewport" content="initial-scale=1.0 height=device-height" />
    <link rel="stylesheet" href="/stylesheets/blueprint/screen.css" type="text/css" media="screen, projection" />
    <link rel="stylesheet" href="/stylesheets/blueprint/print.css" type="text/css" media="print" />
    <!--[if lt IE 8]>
      <link rel="stylesheet" href="css/blueprint/ie.css" type="text/css" media="screen, projection">
    <![endif]-->
    <%= stylesheet_link_tag "application.css" %>
    <%= javascript_include_tag "jquery", "jquery-ui-1.8.custom.min", "rails", "application" %>
    <%#= javascript_tag "$.ajaxSetup({ async: false });" if Rails.env == "test" %>
    <%= csrf_meta_tag %>
    <title>
      <% if content_for? :title %>
        <%= yield :title %>
      <% elsif content_for? :sub_title %>
        <%= yield :sub_title %> - <%= Rails.application.config.app_name %>
      <% else %>
        <%= Rails.application.config.app_name %>
      <% end %>
    </title>
  </head>
  <body>
    <div class="container span-18 noshowgrid">
      <header class="span-18 last">
        <%= render :partial => "/nav/header" %>
      </header>
      <hr/>
      <nav class="span-5 border">
        <%= render :partial => "/nav/nav" %>
      </nav>
      <div class="main span-13 last">
        <div><%= flash[:error] %></div>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
