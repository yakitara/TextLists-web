<%-
  key = calculate_api_key(current_user.salt, "api/items", "create")
-%>
javascript:(function(){
var s=document.createElement('script');
s.src='<%= bookmarklet_url(:format => :js) %>';
document.body.appendChild(s);
})();
